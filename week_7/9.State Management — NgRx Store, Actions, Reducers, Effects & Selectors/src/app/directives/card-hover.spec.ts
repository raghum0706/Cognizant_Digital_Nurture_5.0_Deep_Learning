import { CardHover } from './card-hover';

describe('CardHover', () => {
  it('should create an instance', () => {
    const directive = new CardHover();
    expect(directive).toBeTruthy();
  });
});
